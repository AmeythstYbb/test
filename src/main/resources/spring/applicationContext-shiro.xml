<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context" 
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
		http://www.springframework.org/schema/context  http://www.springframework.org/schema/context/spring-context-3.2.xsd"
	default-lazy-init="true">

	<description>Shiro Configuration</description>

	<!-- 定义 Shiro 主要业务对象 -->
	<bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
		<property name="realm" ref="systemRealm" />
		<!-- 缓存管理器 -->
		<property name="cacheManager" ref="shiroCacheManager" />
	</bean>

	<!-- 项目自定义的 Realm （depend-on用来表示一个Bean的实例化依靠另一个Bean先实例化）。-->
	<bean id="systemRealm" class="com.zlycare.web.boss_24.security.SystemRealm" >

	</bean>
	
	<!-- Shiro 过滤器拦截 -->
	<bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
		<!-- 权限管理器 -->
		<property name="securityManager" ref="securityManager" />
		<!-- 登录地址 -->
		<property name="loginUrl" value="/user/login" />
		<!-- 登录后跳转到业务页面 -->
		<property name="successUrl" value="/index" />
		<!-- 错误页面 -->
		<property name="unauthorizedUrl" value="/unauthorized"/>
		<!-- 鉴权过滤器 -->
		<property name="filters">
			<map>
				<entry key="logout" value-ref="logoutFilter" />
				<entry key="authc" value-ref="myAuthenticationFilter" />
			</map>
		</property>
		<!-- 权限配置 -->
		<property name="filterChainDefinitions">
			<value>
				<!-- 对静态资源不需要进行认证 -->
				/resources/dwz/** = anon
				/resources/js/** = anon
				/resources/mztreeview/** = anon
				/resources/treeTable/** = anon
				/resources/jquery-jbox/** = anon
				/resources/diyUpload/** = anon
				/resources/imgUp/** = anon
				/user/login = authc
				<!-- 退出不需要验证 -->
				/user/logout = logout
				<!-- 对所有url都需要进行认证 -->
				/** = authc
		 	</value>
		</property>
	</bean>

	<!-- 重写的认证filter-->
	<bean id="myAuthenticationFilter" class="com.zlycare.web.boss_24.security.MyAuthenticationFilter" />


	<!-- 使用shiro提供的logout filter -->
	<bean id="logoutFilter" class="org.apache.shiro.web.filter.authc.LogoutFilter">
		<property name="redirectUrl" value="/user/login" />
	</bean>

	<!-- 用户授权信息Cache, 采用EhCache 创建缓存实列-->
	<bean id="shiroCacheManager" class="org.apache.shiro.cache.ehcache.EhCacheManager">
		<property name="cacheManagerConfigFile" value="classpath:cache/ehcache-shiro.xml"/>
	</bean>
	
	<!-- 保证实现了Shiro内部lifecycle函数的bean执行 -->
	<!-- Shiro生命周期处理器 -->
	<bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>
	
	<!-- AOP式方法级权限检查 --> 
	<bean class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator" depends-on="lifecycleBeanPostProcessor">
		<property name="proxyTargetClass" value="true" />
	</bean>
	<bean class="org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor">
    	<property name="securityManager" ref="securityManager"/>
	</bean>
</beans>